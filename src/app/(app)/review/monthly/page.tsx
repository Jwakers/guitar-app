import { MonthlyReviewView } from "@/components/progress/monthly-review-view";

export const metadata = {
  title: "Monthly Review",
};

export default function MonthlyReviewPage() {
  return <MonthlyReviewView />;
}
